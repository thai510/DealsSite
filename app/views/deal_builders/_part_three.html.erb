<%= content_for :rightlayout do %>
<h1>Step 3: When & Where </h1>
<%= form_for(@deal_builder) do |f| %>
  <% if @deal_builder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deal_builder.errors.count, "error") %> prohibited this deal_builder from being saved:</h2>

      <ul>
      <% @deal_builder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div>
<%= f.label 'Offer Launch Date' %>
<%= f.text_field :offer_launch_date %>
</div>

<%= f.label 'Locations to include for deals' %>
         <% User.find(current_user.id).locations.each do |loc| %>
         <%= check_box_tag  'deal_builder[location_ids][]',loc.id,@deal_builder.locations.include?(loc) %>
         <%= loc.name %>
         <% end %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%end%>

