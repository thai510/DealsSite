<%= content_for :rightlayout do %>

<h1> Step 5: Incentives </h1>
<%= form_for(@deal_builder) do |f| %>
  <% if @deal_builder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deal_builder.errors.count, "error") %> prohibited this deal_builder from being saved:</h2>

      <ul>
      <% @deal_builder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div>
<%= f.label 'Ideas for Incentives' %>
         <% IncentiveIdea.all.each do |i_i| %>
         <%= check_box_tag  'deal_builder[incentive_idea_ids][]',i_i.id,@deal_builder.incentive_ideas.include?(i_i) %>
         <%= i_i.description %>
         <% end %>
</div>

<div>
<%= f.label 'Other Incentives' %>
<%= f.text_area :optional_incentive, :size => "20x5" %>
</div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%end%>

