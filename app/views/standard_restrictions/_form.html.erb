<%= form_for(@standard_restriction) do |f| %>
  <% if @standard_restriction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@standard_restriction.errors.count, "error") %> prohibited this standard_restriction from being saved:</h2>

      <ul>
      <% @standard_restriction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title ,:size => '20'%>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :size => '30x5' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
